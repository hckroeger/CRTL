version: 2
updates:
  # Keep the npm dev tooling (Vite, icon/font packages) current.
  - package-ecosystem: npm
    directory: "/"
    # Bumps flow through the develop integration branch, not straight to main.
    target-branch: "develop"
    schedule:
      interval: weekly
    commit-message:
      prefix: deps
    groups:
      # One PR per week for the routine dev-tooling bumps instead of a flood.
      dev-dependencies:
        dependency-type: development

  # Keep the GitHub Actions in our workflows current.
  - package-ecosystem: github-actions
    directory: "/"
    target-branch: "develop"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
